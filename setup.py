from setuptools import setup, find_packages


setup(
   name="nqn_common_base",
   version="0.1.0",
   description="A helper for general operations used by NQN, without any internal imports",
   author='Blue',
   url="https://nqn.blue/",
   packages=find_packages(),
   install_requires=[]
)
